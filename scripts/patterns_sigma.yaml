# Normalization patterns for Sigma rules and engine
"%CommandLine%": "{event_data.process.command_line}"
"%community_id%": "{network.community_id}"
"%CurrentDirectory%": "{event_data.process.working_directory}"
"%document_id%": "{soc_id}"
"%dst_port%": "{destination.port}"
"%dns.query.name%": "{dns.query_name}"
"%dns.resolved_ip%": "{dns.resolved_ip}"
"%hostname%": "{event_data.host.name}"
"%Image%": "{event_data.process.executable}"
"%ImageLoaded%": "{dll.name}"
"%ParentImage%": "{event_data.process.parent.executable}"
"%file.path%": "{event_data.file.path}"
"%TargetFilename%": "{event_data.file.name}"
"%ParentProcessGuid%": "{event_data.process.parent.entity_id}"
"%private_ip%": "{network.private_ip}"
"%ProcessGuid%": "{event_data.process.entity_id}"
"%public_ip%": "{network.public_ip}"
"%related.hosts%": "{event_data.related.hosts}"
"%related_ip%": "{related.ip}"
"%rule.name%": "{rule.name}"
"%src_ip%": "{source.ip}"
"%dst_ip%": "{destination.ip}"
"%User%": "{event_data.user.name}"
